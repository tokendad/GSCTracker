# GSCTracker v2.0 Environment Configuration
# Copy this file to .env and fill in your values

# Server Configuration
PORT=3000
NODE_ENV=development
DATA_DIR=/data

# Session Configuration
# IMPORTANT: Change this to a secure random string in production!
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-this-to-a-secure-random-secret-in-production

# CORS Configuration
# Set to your frontend URL in production (e.g., https://yourdomain.com)
CORS_ORIGIN=http://localhost:3000

# Google OAuth Configuration (Optional)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID=your_google_client_id_here
# GOOGLE_CLIENT_SECRET=your_google_client_secret_here
# GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Email Configuration (Future - for email verification and password reset)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASSWORD=your_smtp_password
# EMAIL_FROM=GSCTracker <noreply@gsctracker.com>

# Database Configuration (Optional)
# By default uses SQLite in DATA_DIR
# Future: PostgreSQL support
# DATABASE_URL=postgresql://user:password@localhost:5432/gsctracker
